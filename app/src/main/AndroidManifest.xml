<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <instrumentation
        android:name=".PrivilegedProcess"
        android:targetPackage="${applicationId}" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".ImsApp"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault.DayNight"
        tools:ignore="MissingApplicationIcon">

        <activity
            android:name=".PatchPromptActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/PromptTheme"
            tools:ignore="ExportedActivity" />

        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".PatchPromptReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.xiaoyueyoqwq.ims.ACTION_APPLY" />
                <action android:name="io.github.xiaoyueyoqwq.ims.ACTION_DISMISS" />
                <action android:name="io.github.xiaoyueyoqwq.ims.ACTION_PAIR" />
            </intent-filter>
        </receiver>

        <service
            android:name=".boot.WirelessAdbJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name=".PromptService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Stay awake until Wireless debugging can apply the 5G+ carrier-config patch" />
        </service>

        <service
            android:name=".PatchApplyService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Apply IMS carrier-config after the user submits a wireless debugging pairing code" />
        </service>
    </application>

    <overlay
        android:requiredSystemPropertyName="ro.boot.vbmeta.device_state"
        android:requiredSystemPropertyValue="locked"
        android:targetPackage="${applicationId}" />

</manifest>
